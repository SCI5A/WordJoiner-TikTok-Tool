# WordJoiner — تقرير التدقيق الشامل

**تاريخ التدقيق:** 22 أغسطس 2026

**النطاق:** مستودع `WordJoiner-TikTok-Tool` كاملًا، بما في ذلك المعالجة، Unicode والعربية، محرك الآيات، DOCX/PDF، Local Storage، الواجهة، PWA، SEO، الأمن، الاختبارات، والنشر الثابت على GitHub Pages.

## الملخص التنفيذي

WordJoiner هو تطبيق PWA ثابت يعمل بالكامل في المتصفح. لا يرفع النصوص إلى خادم، ولا يحتوي على backend أو قاعدة بيانات أو مفاتيح سرية أو خدمة تسجيل مركزية. بعد التدقيق والإصلاحات، نجحت اختبارات المعالجة العربية، النصوص المختلطة، محارف Unicode، محرك اكتشاف الآيات، التصدير إلى DOCX، ورقة الطباعة الخاصة بـ PDF، الحفظ المحلي، الفلترة، الوضع الداكن/الفاتح، وحماية مخرجات السجل من HTML غير موثوق.

اكتُشفت مشكلة مؤكدة في الحفاظ الحرفي على النص المشكّل: كانت `repairArabicSpacing()` تطبق NFC على كامل النص، ما قد يعيد ترتيب بعض combining marks العربية رغم بقاء النص canonical-equivalent. أزيل هذا التطبيع من مسار الإخراج، وأُبقي NFC داخل المقارنة المعجمية فقط. أضيف اختبار انحدار يثبت الحفاظ على ترتيب المحارف، ونجح مع بقية اختبارات الآيات.

أضيفت كذلك حزمة تحقق خفيفة بلا اعتماديات خارجية، و`package-lock.json`، وسير عمل GitHub Actions، وتحسينات SEO، وإعادة تمكين تكبير الصفحة على الهاتف، وأسماء وصول صريحة للأزرار الأيقونية وأزرار السجل. أضيف formatter مستقل يعالج الحروف العربية المتباعدة والتقسيمات المعجمية المؤكدة، ويحمي الآيات والروابط والمنشن والهاشتاغ، مع خيار تشغيل وإيقاف التنسيق الذكي من الواجهة. كما أصبح تثبيت Service Worker لا يفشل إذا تعذّر تحميل خط أو مكتبة خارجية اختيارية؛ الأصول المحلية الأساسية هي التي يفرضها التثبيت، بينما تُخزّن الأصول الخارجية بصورة best-effort.

## المعمارية ومسار البيانات

نقطة الدخول هي `index.html`، والتصميم المتجاوب في `style.css`، والمنطق الرئيسي في `script.js`. يبدأ المسار من textarea الإدخال، ثم يمرر النص إلى `arabic-text-formatter.js` كطبقة تنسيق عربية ذكية مستقلة، ثم يستعمل `arabic-spacing.js` كمسار fallback للتوافق، وبعدها يحافظ على حدود الكلمات بإضافة U+2060 أو الطريقة التي يختارها المستخدم، ثم يكتب الناتج في textarea الإخراج. لا تُضاف محارف الحماية داخل الكلمة نفسها، لذلك لا ينبغي أن يعود السلوك السابق الذي كان يُظهر الحروف العربية متباعدة مثل `ف إ ذ ا`.

يُخزّن سجل النصوص والإعدادات محليًا في `localStorage` تحت مفاتيح التطبيق. هذا التخزين مرتبط بأصل الصفحة ولا يُرسل البيانات إلى جهة خارجية. تستخدم واجهات السجل `escapeHtml()` قبل إدراج بيانات المستخدم في `innerHTML`، بينما تستخدم المعاينة والتصدير عقدًا نصية أو قيمًا ثابتة؛ ولم يظهر عنصر HTML تنفيذي في اختبار XSS.

يعيد `quran-segmentation.js` مصفوفة من المقاطع المنظمة بدل سلسلة واحدة. المقطع المؤكد المحاط بـ `﴿﴾` يحمل `type: "ayah"` و`confidence: 1` و`source: "quran-marker"`. أما الاكتشاف السياقي فيحمل ثقة أقل ولا يُعامل كتحقق من corpus كامل. يبني `docx-export.js` حزمة OpenXML محلية، ويستخدم PDF ورقة RTL مؤقتة يفتحها المستخدم في حوار الطباعة الخاص بالمتصفح.

## مصفوفة التحقق

| الفئة | الأمر أو السيناريو | النتيجة | الملاحظات |
|---|---|---:|---|
| البنية الثابتة | `npm run build` | ناجح | يتحقق من الأصول وRTL وSEO وformatter ونسخة SW v15. |
| الاختبارات | `npm test` | ناجح | يشغّل التحقق واختبارات formatter وArabic spacing وQuran القصيرة والطويلة. |
| الصياغة | `npm run lint` و`node --check` | ناجح | لا أخطاء Syntax في ملفات JavaScript. |
| الأمن الاعتمادي | `npm run security` | ناجح | `npm audit --audit-level=high --omit=dev` أبلغ عن 0 vulnerabilities. |
| TypeScript | `npm run typecheck` | غير منطبق بنجاح | المشروع Vanilla JavaScript؛ الأمر يوضح ذلك صراحة ولا يدّعي فحصًا غير موجود. |
| اختبار الآيات الطويل | `quran-segmentation.long.test.js` | ناجح | 106,389 محرفًا، 800 فقرة، 320 علامة آية، 0 إيجابيات عادية كاذبة، نحو 5–7 ms. |
| Smart formatter | `arabic-text-formatter.test.js` | ناجح | يغطي الحروف المتباعدة، الكلمات الملصقة المؤكدة، punctuation، RTL/LTR، emoji، الروابط، المنشن، الآيات، tabs وفواصل الأسطر. |
| Unicode والتشكيل | `arabic-spacing.test.js` | ناجح | يثبت code-point sequence للنص المشكّل والمختلط والرموز وemoji. |
| DOCX | `unzip -t` وقراءة `word/document.xml` | ناجح | ZIP صالح، RTL حاضر، الآية في run مستقل ملوّن، ولا يوجد U+2060. |
| PDF | اعتراض آمن لـ `window.print()` وفحص `#pdfPrintSheet` | ناجح | RTL، مقطع آية، لا محارف حماية، ولا script nodes. الحفظ النهائي من حوار النظام يدوي. |
| Local Storage | تحويل ثم حفظ تلقائي ثم بحث | ناجح | سجل محلي، history، وفلترة مطابقة تعمل دون رفع النص. |
| XSS regression | `<img src=x onerror=alert('xss')>` في السجل | ناجح | ظهر كنص حرفي، `img` و`script` داخل السجل = صفر. |
| PWA | Service Worker على الخادم المحلي | ناجح | worker مفعّل ويتحكم بالصفحة، وcache `wordjoiner-pro-v15` يحتوي الأصول المحلية وformatter v2. |
| Git diff | `git diff --check` | ناجح | لا أخطاء whitespace في التغييرات الحالية. |

## المشكلات المؤكدة وإجراءات الإصلاح

| الشدة | المجال | السبب الجذري | الإصلاح المنفذ | دليل الانحدار |
|---|---|---|---|---|
| عالية | Unicode/العربية | تطبيق `normalize('NFC')` على النص المعروض كان يعيد ترتيب combining marks. | أصبح مسار الإخراج يحافظ على النص كما هو؛ NFC بقي للمقارنة المعجمية فقط. | اختبار تشكيل حرفي ناجح. |
| متوسطة | Smart formatting | لم توجد طبقة مستقلة لترميم نصوص TikTok/PDF والنسخ المختلط قبل الحماية. | أضيف `arabic-text-formatter.js` قبل Quran detection وWord Joiner، مع toggle واختبارات حماية للآيات والروابط والمنشن والهاشتاغ. | `arabic-text-formatter.test.js` واختبار المتصفح. |
| متوسطة | Local Storage initialization | كان `MAX_SAVED_TEXTS` يُقرأ قبل تهيئته بسبب TDZ أثناء `initializeApp()`. | نقل الثابت قبل استدعاء التهيئة. | إعادة تحميل المتصفح مع سجل محفوظ بلا console error أو حذف للبيانات. |
| عالية | قابلية التحقق | لم يكن هناك package manifest أو أوامر قابلة لإعادة التشغيل أو CI. | أضيف `package.json` بلا اعتماديات، lockfile، `scripts/validate-static.js`، و`.github/workflows/validate.yml`. | `npm ci` ثم `npm test` ناجحان. |
| متوسطة | PWA | `cache.addAll()` على أصول خارجية اختيارية كان يمكن أن يجعل تثبيت worker يفشل بالكامل عند فشل شبكة CDN. | فُصلت `CORE_ASSETS` المحلية عن `OPTIONAL_ASSETS`، وتُخزّن الخارجية بصورة catch/best-effort، مع cache v15. | Service Worker مفعّل وcache المحلي مكتمل. |
| منخفضة | SEO | غياب `robots.txt` و`sitemap.xml` وcanonical وOpen Graph وTwitter cards. | أضيفت الملفات والوسوم مع URL GitHub Pages الصحيح. | static validation ووجود الملفات في cache. |
| منخفضة | الوصول | viewport كان يمنع التكبير، وبعض الأزرار الديناميكية اعتمدت على title فقط. | أزيل `user-scalable=no`، وضُبط `maximum-scale=5.0`، وأضيفت `aria-label` لأزرار الرأس والسجل. | فحص DOM: 46 عنصرًا قابلًا للتركيز، وأزرار السجل بأسماء وصول صريحة. |

## الأمن والخصوصية

لم يظهر في المستودع سر مكشوف أو مفتاح API أو endpoint يرفع النصوص. التعامل مع النصوص محلي. المخاطر الرئيسية كانت في العرض الديناميكي، وقد اختُبر إدخال HTML-like في المعالجة والسجل؛ لم ينتج عنصر `img` أو `script` ولم يُنفذ handler. تظل قاعدة الأمان المهمة هي استمرار تمرير البيانات إلى `textContent` أو `escapeHtml()` وعدم إدخال بيانات المستخدم الخام في HTML أو inline handlers جديدة.

يعتمد السجل على `localStorage`، وهو تخزين دائم خاص بأصل الصفحة، وليس مكانًا مناسبًا لبيانات شديدة الحساسية أو جهاز مشترك. أُبقيت واجهة مسح السجل والبيانات، وعلى المستخدم مسح بيانات المتصفح إذا كان الجهاز مشتركًا. هذا يتفق مع طبيعة Web Storage المرتبطة بأصل المستند واستمرار البيانات بين جلسات المتصفح.[1]

## PWA والأداء

يستخدم التطبيق Service Worker مع `skipWaiting()` و`clients.claim()` وحذف caches القديمة. الأصول الحرجة المحلية تُخزّن أثناء التثبيت، بينما الخطوط وFont Awesome موارد اختيارية؛ لذلك قد يعود العرض إلى system fonts دون أن يتوقف التطبيق. توصي دورة حياة Service Worker بتعبئة cache أثناء install وتنظيف الموارد القديمة أثناء activate، كما أن فشل مورد مطلوب يمكن أن يفشل التثبيت؛ لذلك كان فصل الأصول الاختيارية إصلاحًا وقائيًا مناسبًا.[2]

اختبار formatter قاس أيضًا معالجة النصوص متعددة الأسطر وحافظ على الآيات المعلّمة دون تعديل داخلي أو NFC. اختبار النص الطويل قاس محرك التقسيم على 106,389 محرفًا و800 فقرة في نحو 5–7 ms داخل Node، مع 320 آية معلّمة و0 إيجابيات كاذبة عادية. لا توجد قاعدة Quran corpus كاملة محلية، ولذلك لا يجوز تفسير الثقة السياقية البالغة `0.72` على أنها تحقق نصي من مصدر قرآني كامل. العلامة `﴿...﴾` هي مسار الثقة الأعلى.

## الواجهة وتجربة الاستخدام

الواجهة RTL وتبدأ بالوضع الداكن، وتدعم التبديل إلى الوضع الفاتح مع حفظ الاختيار. مسار التحويل والتراجع والمعاينة والحفظ والبحث والفلترة والتصدير ظهر في المتصفح. إعادة بناء الإدخال بعد إزالة محارف الحماية أعادت النص المختلط حرفيًا مع line breaks وemoji والآية. أُعيد تمكين zoom على الهاتف، وتبقى عناصر التحكم قابلة للتركيز عبر لوحة المفاتيح.

أُجري تحقق متصفح مكتبي محلي. شمل التحقق toggle الخاص بالتنسيق الذكي: ظل النص المباعد كما هو عند الإيقاف، وأُصلح إلى كلمات كاملة عند التشغيل، مع حفظ الحالة في `wordjoiner_settings`. لم تُنفذ محاكاة جهاز هاتف مستقل عبر DevTools في هذه الجولة، لكن CSS responsive موجود، وتغيير viewport السابق لم يكشف كسرًا وظيفيًا؛ يجب إبقاء اختبار جهاز فعلي ضمن QA قبل تغييرات تصميم كبيرة. كما أن حوار Save as PDF نفسه تابع للنظام ولا يمكن ضمانه آليًا من داخل الصفحة؛ الذي أمكن اختباره آليًا هو ورقة الطباعة التي يراها المستخدم قبل الحفظ.

## SEO والنشر

أضيفت `robots.txt` مع رابط sitemap، و`sitemap.xml` لصفحة GitHub Pages، ووسوم description وcanonical وOpen Graph وTwitter card. ملف robots لإدارة وصول crawler وليس وسيلة لإخفاء الصفحة من نتائج البحث، وsitemap يساعد محركات البحث على اكتشاف الصفحات لكنه لا يضمن الفهرسة.[3] [4]

لا يزال الموقع تطبيق صفحة واحدة ولا يحتاج sitemap متعدد الصفحات حاليًا. يجب تحديث `sitemap.xml` إذا أضيفت صفحات عامة جديدة، وتحديث `CACHE_NAME` عند تغيير أصول المعالجة أو الواجهة.

## القيود المتبقية

لا يوجد corpus قرآني كامل للتحقق من كل نص غير معلّم؛ لذلك يجب إبقاء تحذير الثقة للمقاطع السياقية وعدم تقديمها للمستخدم على أنها فتوى أو تحقق مصدر. تصدير PDF يعتمد على الطباعة المدمجة بالمتصفح، وليس مولد PDF مستقلًا. كما أن توفر الخطوط الخارجية في وضع عدم الاتصال ليس مضمونًا، مع وجود fallback محلي وإصلاح فشل التثبيت.

المشروع لا يحتوي TypeScript أو bundler أو backend أو قاعدة بيانات، وهو قرار معماري مناسب لتطبيق privacy-first صغير، لكنه يعني أن `typecheck` غير موجود وأن اختبارات التكامل العميقة للمتصفح تحتاج أداة خارجية إذا أُريدت أتمتتها بالكامل. سير العمل الجديد يغطي التحقق الثابت والوحدات، لا حوار الطباعة الرسومي أو اختبار أجهزة iOS/Android الفعلية.

## توصيات المرحلة التالية

الأولوية الأولى هي إضافة corpus اختياري موثوق أو تكامل مراجعة بشرية للآيات غير المعلّمة، مع إبقاء النص المدخل محليًا. الأولوية الثانية هي إضافة اختبارات متصفح headless قابلة للتشغيل في CI لسيناريوهات paste، Local Storage، XSS، preview، responsive breakpoints، وPDF sheet. الأولوية الثالثة هي تقليل الاعتماد المرئي على CDN عبر self-hosting لخط Amiri وملفات الأيقونات، ثم إضافة صفحة إعدادات تصدير قابلة للحفظ والنسخ الاحتياطي المحلي بصيغة JSON.

من المفيد أيضًا إضافة زر “نسخ نظيف” يزيل محارف الحماية قبل النسخ عند الحاجة، مع إبقاء الزر الحالي المتوافق مع TikTok. ويمكن لاحقًا إضافة تسمية واضحة للثقة، دعم قوالب نصية للمحتوى الاجتماعي، ومقارنة قبل/بعد على مستوى grapheme clusters، شرط ألا يؤدي ذلك إلى تطبيع نص المستخدم أو تغييره دون إذن.

## المراجع

[1]: https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage "MDN: Window.localStorage"
[2]: https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API/Using_Service_Workers "MDN: Using Service Workers"
[3]: https://developers.google.com/search/docs/crawling-indexing/robots/intro "Google Search Central: Introduction to robots.txt"
[4]: https://developers.google.com/search/docs/crawling-indexing/sitemaps/overview "Google Search Central: Learn about sitemaps"
